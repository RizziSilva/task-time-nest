export const PASSWORD_MAX_LENGTH: number = 16;

export const PASSWORD_MIN_LENGTH: number = 8;

export const SPECIAL_CHARACTERS_REGEX: RegExp = new RegExp(/[!"#$%&'()*+,-./:;<=>?@[\]^_`{|}~\\]/);

export const LETTER_REGEXP: RegExp = new RegExp(/[A-Z]/);

export const UPPERCASE_LETTER_REGEXP: RegExp = new RegExp(/[A-Z]/);

export const DIGIT_REGEXP: RegExp = new RegExp(/[0-9]/);
